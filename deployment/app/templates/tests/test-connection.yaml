apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "scorecard.fullname" . }}-test-connection"
  labels:
    {{- include "scorecard.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "scorecard.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
